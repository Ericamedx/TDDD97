innan allt: 

var i samma mapp som sqlite3 i cmd
Skriv: sqlite3 database.db < schema.sql

console: vara i lab3\Twidder

set FLASK_APP=Twidder

(oklart) set FLASK_ENV=development

pip install -e .
flask run